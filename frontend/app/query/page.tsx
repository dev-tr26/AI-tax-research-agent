import { QueryInterface } from "@/components/query/QueryInterface";

export const metadata = { title: "Research Query — TaxAI" };

export default function QueryPage() {
  return <QueryInterface />;
}
